import { Pre, Code } from "@/app/components/typography";
import CodeTabs from "@/app/components/ui/code-tabs";
import { Alert, AlertTitle, AlertDescription } from "@/app/components/ui/alert";
import { TriangleAlertIcon, InfoIcon } from "lucide-react";
import { ApiKeyField } from "@/app/dev/components/api-key-field";
import { getOrCreateApiKey } from "@/app/api/api-key/api-key-data-layer";
import { SubdomainLink } from "@/app/components/subdomain-link";

export const metadata = {
title: "OpenClaw",
};

# OpenClaw with Synthetic

Synthetic is a native provider for [OpenClaw](https://www.openclaw.ai/), the self-hosted, agentic LLM bot!

With Synthetic, you can use OpenClaw with any [model we support](/docs/api/models),
including our high quality self-hosted models.

## Install OpenClaw

Follow the official guide to [Install OpenClaw](https://docs.openclaw.ai/start/getting-started).

## Quick Setup

Getting started with OpenClaw and Synthetic takes just a few simple steps:

### 1. Open OpenClaw in Your Terminal

```bash
openclaw
```

### 2. Select an Onboarding Mode

Select **QuickStart**.

![Select an Onboarding Mode - QuickStart](/images/dev/docs/moltbot/moltbot-onboarding.png)

### 3. Choose "Synthetic"

Select **Synthetic** from the list of available providers.

![Select Synthetic](/images/dev/docs/moltbot/moltbot-provider.png)

### 4. Paste Your Synthetic API Key

Select **Synthetic API Key** and enter your [API Key](/docs/api/overview).

<div className="pb-4">
  <ApiKeyField
    initResp={await getOrCreateApiKey()}
    wrapperClassName="border-gray-300 text-gray-900"
  />
</div>

![Enter your Synthetic API Key](/images/dev/docs/moltbot/moltbot-apikey.png)

### 5. Choose a Model

Select which model you want to use from our [available models](/docs/api/models).

<Alert className="mb-4">
  <InfoIcon color="#00bb00" />
  <AlertTitle>Synthetic Hosted Models</AlertTitle>
  <AlertDescription>
    <p>
      We recommend Synthetic hosted models for maximum quality and performance.
    </p>
  </AlertDescription>
</Alert>


![Choose the desired open-source model.](/images/dev/docs/moltbot/moltbot-model.png)

### 6. You're All Set!

Finish the rest of the onboarding flow to get started with OpenClaw!

## Getting Help

- **Community**: Join our <SubdomainLink href="/discord">Discord</SubdomainLink>! The Synthetic team is active daily.
- **E-mail**: See <SubdomainLink href="/user-settings/contact">Contact Us</SubdomainLink>